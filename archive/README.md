# Arquivo do projeto

Ficheiros **não usados no produto** atual (web PWA + API), mantidos como referência histórica.

| Ficheiro | Notas |
|----------|--------|
| [`battery_app.py`](battery_app.py) | Protótipo desktop tkinter + pygame + numpy. A lógica de som procedural viva está em [`../frontend/src/audio/engine.ts`](../frontend/src/audio/engine.ts). |

Para correr o legado (opcional): `pip install pygame numpy` e `python battery_app.py` a partir desta pasta.
